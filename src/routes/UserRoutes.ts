/*import { Router } from "express";
import { UserControllers } from "../controllers/UserControllers";

const routerUser = Router();
const user = new UserControllers();

routerUser.get("/",user.handleListUser);

routerUser.get("/add", (request, response) => {
  response.render("add");
});
routerUser.get("/index", (request, response) => {
  response.render("index");
});
/*router.get("/index", (request, response) => {
  response.render("index");
});*/
/*routerUser.post("/add-user", user.handleCreateUser);

routerUser.get("/search", user.handleSearchUser);

routerUser.get("/edit", user.handleGetUser);

routerUser.post("/edit-user", user.handleUpdateUser);

routerUser.post("/delete-user", user.handleDeleteUser);
export { routerUser };

*/