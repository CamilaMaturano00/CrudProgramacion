/*import { Router } from "express";
import ProductControllers from "../controllers/ProductControllers";

const routerProduct = Router();
const product = new ProductControllers();

routerProduct.get("/", product.handleListProduct);
routerProduct.get("/addProduct", product.handleAddProduct);
routerProduct.post("/addProduct", product.handleCreateProduct);
routerProduct.get("/search", product.handleSearchProduct);
routerProduct.get("/editProduct", product.handleGetProduct);
routerProduct.post("/edit-product", product.handleUpdateProduct);
routerProduct.post("/delete-product", product.handleDeleteProduct);

export { routerProduct };
*/